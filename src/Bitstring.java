import com.javafx.tools.doclets.formats.html.SourceToHTMLConverter;import java.util.Scanner;/** * Created by Hoshiko on 11/5/15. */public class Bitstring {    private static Scanner scanner = new Scanner(System.in);    public static void main (String [] args){        int numValue = 0;        System.out.println("Please enter an interger beyween 0 and 255");        numValue = scanner.nextInt();        if(numValue > 255 || numValue < 0){            System.out.println("Error : Interger is too big or too small");        }        getBinary(numValue);        countOnes(numValue);        System.out.println(isPalindrome(numValue));    }    private static void getBinary(int numValue) {        String s = getBitstring(numValue);        System.out.println("BitString equivalent is " + s);    }    private static String getBitstring (int numValue){        String s = "";        for(int j = 0; j < 8; j++ ){            if(numValue % 2 == 1){                s = '1' + s;            }            if(numValue % 2 == 0){                s = '0' + s;            }            numValue = numValue /2;        }        return s;    }    private static void countOnes (int num){        String number = getBitstring(num);        int count=0;        boolean notValid = false; //if the number is valid        do {            for (int i = 0; i <= number.length()-1; i++){                char c = number.charAt(i);                if(c == '0' || c == '1'){                    if (c == '1'){                        count++;                        if(count > 2){                            notValid = true;                            break; //<-- break the for loop, because the condition for the number of '1' is not satisfied                        }                    }                }                else {                    notValid = true; // <-- the character is not 0 or 1 so it's not a binary number                    break;                }            }    }while(notValid);  //<-- while the condition is not reached, re-ask for user input        System.out.println("Count of 1 is " + count);    }    private static boolean isPalindrome(int number) {        String s = getBitstring(number);        number = Integer.parseInt(s);        if(number == reverse(number)){            return true;        }        return false;    }    private static int reverse(int number){        int reverse = 0;        while(number > 0){            reverse = reverse*10 + number%10;            number = number/10;        }        return reverse;    }}